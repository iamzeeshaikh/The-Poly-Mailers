---
import { whatsappLink } from '~/lib/site';

interface Props {
  /** Page-specific pre-filled message. */
  message?: string;
}

const {
  message = 'Hi, I found The Poly Mailers online and would like a quotation for poly mailers.',
} = Astro.props;

const href = whatsappLink(message);
---

<!--
  Fixed bottom-LEFT so it never sits over a live chat launcher, a cookie
  control or the primary action on a form. Reserved size, so it cannot
  contribute to layout shift.
-->
<a class="wa-float" href={href} target="_blank" rel="noopener" aria-label="Message The Poly Mailers on WhatsApp about this page">
  <svg width="26" height="26" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
    <path
      fill="currentColor"
      d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.46 1.32 4.96L2 22l5.25-1.38a9.9 9.9 0 0 0 4.79 1.22h.01c5.46 0 9.91-4.45 9.91-9.91S17.5 2 12.04 2Zm0 18.15h-.01a8.2 8.2 0 0 1-4.19-1.15l-.3-.18-3.12.82.83-3.04-.2-.31a8.19 8.19 0 0 1-1.26-4.38c0-4.54 3.7-8.23 8.25-8.23a8.23 8.23 0 0 1 0 16.46Zm4.52-6.16c-.25-.12-1.47-.72-1.69-.81-.23-.08-.39-.12-.56.13-.16.24-.64.8-.79.97-.14.16-.29.18-.54.06-.25-.13-1.05-.39-1.99-1.23-.74-.66-1.23-1.47-1.38-1.72-.14-.25-.01-.38.11-.5.11-.11.25-.29.37-.44.13-.14.17-.25.25-.41.09-.17.04-.31-.02-.44-.06-.12-.56-1.35-.77-1.84-.2-.49-.4-.42-.55-.43h-.47c-.16 0-.43.06-.65.31-.23.25-.86.84-.86 2.05s.88 2.38 1 2.54c.13.17 1.74 2.65 4.2 3.72.59.25 1.05.4 1.4.52.59.19 1.13.16 1.55.1.47-.07 1.47-.6 1.68-1.18.2-.58.2-1.07.14-1.18-.06-.11-.22-.17-.47-.29Z"></path>
  </svg>
  <span class="visually-hidden">Chat on WhatsApp</span>
</a>

<style>
  .wa-float {
    position: fixed;
    left: max(1rem, env(safe-area-inset-left));
    bottom: max(1rem, env(safe-area-inset-bottom));
    z-index: 90;
    inline-size: 54px;
    block-size: 54px;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: #1fa855;
    color: #fff;
    box-shadow: 0 6px 20px rgba(13, 17, 22, 0.24);
    transition: transform var(--dur) var(--ease), background var(--dur-fast) var(--ease);
  }

  .wa-float:hover { background: #17843f; transform: translateY(-2px); }
  .wa-float:active { transform: translateY(0); }

  @media (max-width: 30rem) {
    .wa-float { inline-size: 50px; block-size: 50px; }
    .wa-float svg { width: 23px; height: 23px; }
  }

  @media print { .wa-float { display: none; } }
</style>
