User-agent: *
Allow: /

# Nothing here is a crawl trap, but these are not useful to index.
Disallow: /api/
Disallow: /thank-you/

# No internal search exists on this site, so there are no search-result URLs
# to exclude. Query parameters are not used for navigation anywhere, and every
# indexable page has a self-referencing canonical.

Sitemap: https://thepolymailers.com/sitemap.xml
